// Instruction authoring template. Copy this file when semantics are approved.
// Keep the template commented so the repository contains no implementation.
//
// func INSTRUCTION_NAME(operands...) => ResultType
// begin
//     // Assert architecture-level legality conditions.
//     // Compute the architecture-visible result or state transition.
//     return result;
// end;

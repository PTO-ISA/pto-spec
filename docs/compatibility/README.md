# Compatibility appendices

`scalar/` is the pinned 1105-page Linx/DavinciOO scalar micro-ISA compatibility
snapshot.  It is not part of the 106-operation Tile catalog and does not add
PTO Tile operations.  `spec/evidence/source-locks/` records its provenance.

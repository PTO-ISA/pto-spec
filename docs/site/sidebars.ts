import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/**
 * Reference navigation follows the generated documentation tree. ASL-backed
 * instruction workbench routes are generated by the PTO content plugin and
 * must not be manually enumerated here.
 */
const sidebars: SidebarsConfig = {
  reference: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;

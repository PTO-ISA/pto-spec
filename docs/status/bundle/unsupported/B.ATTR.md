---
{
  "schema_version": 1,
  "id": "header.header-b.attr",
  "kind": "header",
  "title": "B.ATTR",
  "status": "removed",
  "visibility": "internal",
  "profile": "pto-isa-0.58.0",
  "family": "Unsupported Block/Header",
  "sources": {
    "davincioo": "header/B.ATTR.md"
  }
}
---
# B.ATTR（兼容跳转）

> **NON-NORMATIVE / UNSUPPORTED**  
> This document is not part of PTO ISA 0.58.0 and must not be used as assembler, decoder, ASL, encoding, or implementation input.

该页面只用于兼容旧链接，不属于 v5 active header 集合。

`B.ATTR` 不是当前 live `v0.56` 描述符。规范拆分如下：

- `B.CATR`：块控制属性
- `B.DATR`：块数据属性

新的 `v0.56` golden、文档和工具定义不应继续使用 `B.ATTR`。

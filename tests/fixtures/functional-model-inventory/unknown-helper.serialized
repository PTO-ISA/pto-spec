let open AST in let annot = ASTUtils.add_dummy_pos in [D_GlobalStorage { name="Counter"; keyword=GDK_Var; ty=Some (annot (T_Int (UnConstrained))); initial_value=Some (annot (E_Literal (L_Int (Future.of_string "0")))) }]

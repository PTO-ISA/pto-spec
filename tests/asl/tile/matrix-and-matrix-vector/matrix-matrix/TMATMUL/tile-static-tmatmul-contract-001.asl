// Generated by scripts/generate-mnemonic-avs.py. Do not edit.
// PTO-TEST: {"id":"PTO-AVS-TILE-TMATMUL-STATIC-001","source":"asl/tile/matrix-and-matrix-vector/matrix-matrix/TMATMUL.asl","requirements":["PTO-INST-TILE-TMATMUL"],"kind":"static-invariant","summary":"canonical TMATMUL catalog and ASL contract remain aligned","pass_condition":"all canonical decode and contract assertions hold","related_sources":[]}
func main() => integer
begin
    // classification: matrix-and-matrix-vector/matrix-matrix
    // block: BSTART.TMATMUL AType | B.DATR BType, RMode, Sat (optional; BType defaults to AType) | B.FPATR PreQuantMode, ReluMode, GroupNCode, RowMaxEn, GroupMaxEn, RowMaxInit, MaxAbsEn, TransA, TransB, CScaleEn (exactly one) | B.DIM LB0 M or cooperative group_M (optional, default 1) | B.DIM LB1 N (optional, default 1) | B.DIM LB2 K (optional, default 1) | B.IOS complete right or both matrix operand groups (optional; cooperative mask 1111) | B.IOT ordered Local mathematical sources: A CUBE_M16/M32 primary, B CUBE_N8 primary | B.IOT D matching A's CUBE_M16/M32 layout, optional RowMaxOut, optional GroupMaxOut destinations | B.IOT/B.IOR postprocess operands selected by B.FPATR | BSTOP or the next BSTART completion boundary
    assert DecodeTileOperation(TileDecode_CUBE, '000000000000') == 91;
    assert TileOperationOfIndex(91) == TileOperation_TMATMUL;
    assert TileHandlerOfIndex(91) == TileHandler_TMATMUL;
    assert TileClassOfIndex(91) == TileClass_MatrixAndMatrixVector;
    assert TileEngineOfIndex(91) == TileEngine_CUBE;
    assert InstructionContractOperation_TMATMUL() == TileOperation_TMATMUL;
    assert InstructionContractHandler_TMATMUL() == TileHandler_TMATMUL;
    assert InstructionContractCubeFunction_TMATMUL() == 0;
    assert InstructionContractSharedOperandsAllowed_TMATMUL() == TRUE;
    return 0;
end;

// Generated by scripts/generate-mnemonic-avs.py. Do not edit.
// PTO-TEST: {"id":"PTO-AVS-TILE-ATOM-CAS-SEMANTIC-001","source":"asl/tile/memory-and-data-movement/irregular/ATOM_CAS.asl","requirements":["PTO-INST-TILE-ATOM-CAS"],"kind":"execution","summary":"ATOM_CAS exercises its GM semantic primitive at the owner boundary","pass_condition":"the decoded owner mapping and representative GM effect assertions hold","related_sources":["asl/tile/model/memory/gm-atom-red.asl","asl/tile/model/memory/gm-atom-red-execution.asl"]}
func main() => integer
begin
    assert GMAtomicOperationFromFunction(8) == GMAtomic_CAS;
    assert GMAtomicOperationDataTypeLegal(GMAtomic_CAS, TileDataType_U16);
    let (result, performed) = GMAtomicResult(GMAtomic_CAS, TileDataType_U16,
        Zeros{PTO_XLEN} + 3, Zeros{PTO_XLEN} + 3,
        Zeros{PTO_XLEN} + 3, Zeros{PTO_XLEN} + 9);
    assert result == Zeros{PTO_XLEN} + 9;
    assert performed;
    let (cas_miss, cas_performed) = GMAtomicResult(GMAtomic_CAS,
        TileDataType_U16, Zeros{PTO_XLEN} + 3, Zeros{PTO_XLEN},
        Zeros{PTO_XLEN} + 4, Zeros{PTO_XLEN} + 9);
    assert cas_miss == Zeros{PTO_XLEN} + 3;
    assert !cas_performed;
    return 0;
end;

// PTO-TEST: {"id":"PTO-AVS-BLOCK-RANGE-OPERATION-MATRIX-048","source":"asl/block/model/operands/subview-descriptor.asl","requirements":["PTO-B-SUBVIEW-DESCRIPTOR-001","PTO-BLOCK-TILE-OPERATION-APPLICABILITY-001","PTO-B-ASSEMBLE-PRODUCER-EFFECT-ELIGIBILITY-001"],"kind":"execution","summary":"Generated decoded operation-role cases exercise every required source and destination role through normal dispatch.","pass_condition":"Every case binds its authoritative fixture recipe and catalog-required operands/attributes, reaches normal modifier and applicability preflight, then asserts either its exact normal observable or its exact pre-effect fault.","related_sources":["asl/block/model/operands/subview-descriptor.asl","asl/block/model/operands/range-modifiers.asl","asl/block/model/operands/portable-carriers.asl"]}
// Generated by scripts/generate-bundle-operation-matrix.py; cases: RANGE-MATRIX-CASE-0189, RANGE-MATRIX-CASE-0190, RANGE-MATRIX-CASE-0191, RANGE-MATRIX-CASE-0192
// RANGE-MATRIX-CASE-0189 TSORT source0 -> case_0189_normal_commit
func RunCase_0189() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 1 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_FP16, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                16, 4, 1,
                4, TileDataType_FP16,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0x26c19181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x00105e13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_0 = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert modifier_0_0 == CommandExecution_Executed;
    let bind_1 = ExecuteCommandInstruction(Zeros{64} + 0x0000ee13, 32);
    assert bind_1 == CommandExecution_Executed;
    let bind_2 = ExecuteCommandInstruction(Zeros{64} + 0x0008ee93, 32);
    assert bind_2 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(78);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(78);
    assert SelectedBundleClosedUnarySchemaLegal(78);
    assert SelectedBundleClosedTFMASchemaLegal(78);
    assert SelectedBundleClosedQuantizationSchemaLegal(78);
    assert SelectedBundleClosedGenerationSchemaLegal(78);
    assert SelectedBundleClosedHistogramSchemaLegal(78);
    assert SelectedBundleClosedReductionSchemaLegal(78);
    assert SelectedBundleClosedSortingSchemaLegal(78);
    assert SelectedBundleClosedPartialSchemaLegal(78);
    assert SelectedBundleClosedExpansionSchemaLegal(78);
    assert SelectedBundleClosedTCVTSchemaLegal(78);
    assert SelectedBundleClosedTCMPSchemaLegal(78);
    assert SelectedBundleClosedTSELSchemaLegal(78);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(78);
    assert SelectedBundleClosedTCMPSSchemaLegal(78);
    assert SelectedBundleClosedTSELSSchemaLegal(78);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(78);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0190 TSORT destination0 -> case_0190_normal_commit
func RunCase_0190() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 0 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_FP16, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                16, 4, 1,
                4, TileDataType_FP16,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0x26c19181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x00105e13, 32);
    assert bind_0 == CommandExecution_Executed;
    let bind_1 = ExecuteCommandInstruction(Zeros{64} + 0x0000ee13, 32);
    assert bind_1 == CommandExecution_Executed;
    let assemble_1 = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble_1 == CommandExecution_Executed;
    let bind_2 = ExecuteCommandInstruction(Zeros{64} + 0x0008ee93, 32);
    assert bind_2 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(78);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(78);
    assert SelectedBundleClosedUnarySchemaLegal(78);
    assert SelectedBundleClosedTFMASchemaLegal(78);
    assert SelectedBundleClosedQuantizationSchemaLegal(78);
    assert SelectedBundleClosedGenerationSchemaLegal(78);
    assert SelectedBundleClosedHistogramSchemaLegal(78);
    assert SelectedBundleClosedReductionSchemaLegal(78);
    assert SelectedBundleClosedSortingSchemaLegal(78);
    assert SelectedBundleClosedPartialSchemaLegal(78);
    assert SelectedBundleClosedExpansionSchemaLegal(78);
    assert SelectedBundleClosedTCVTSchemaLegal(78);
    assert SelectedBundleClosedTCMPSchemaLegal(78);
    assert SelectedBundleClosedTSELSchemaLegal(78);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(78);
    assert SelectedBundleClosedTCMPSSchemaLegal(78);
    assert SelectedBundleClosedTSELSSchemaLegal(78);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(78);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    let completed_writer_0 = CompleteBundleLocalGenerationWriterEvent(
        BundleLocalGenerationSlot(0, '1111'), _BundleExecutionDomainToken,
        0, BundleLocalGenerationCellCount(1));
    assert completed_writer_0;
    assert _LocalGenerations[[BundleLocalGenerationSlot(0, '1111')]].published;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0191 TSORT destination1 -> case_0191_normal_commit
func RunCase_0191() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 0 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_FP16, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                16, 4, 1,
                4, TileDataType_FP16,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0x26c19181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x00105e13, 32);
    assert bind_0 == CommandExecution_Executed;
    let bind_1 = ExecuteCommandInstruction(Zeros{64} + 0x0000ee13, 32);
    assert bind_1 == CommandExecution_Executed;
    let bind_2 = ExecuteCommandInstruction(Zeros{64} + 0x0008ee93, 32);
    assert bind_2 == CommandExecution_Executed;
    let assemble_2 = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble_2 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(78);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(78);
    assert SelectedBundleClosedUnarySchemaLegal(78);
    assert SelectedBundleClosedTFMASchemaLegal(78);
    assert SelectedBundleClosedQuantizationSchemaLegal(78);
    assert SelectedBundleClosedGenerationSchemaLegal(78);
    assert SelectedBundleClosedHistogramSchemaLegal(78);
    assert SelectedBundleClosedReductionSchemaLegal(78);
    assert SelectedBundleClosedSortingSchemaLegal(78);
    assert SelectedBundleClosedPartialSchemaLegal(78);
    assert SelectedBundleClosedExpansionSchemaLegal(78);
    assert SelectedBundleClosedTCVTSchemaLegal(78);
    assert SelectedBundleClosedTCMPSchemaLegal(78);
    assert SelectedBundleClosedTSELSchemaLegal(78);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(78);
    assert SelectedBundleClosedTCMPSSchemaLegal(78);
    assert SelectedBundleClosedTSELSSchemaLegal(78);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(78);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    let completed_writer_1 = CompleteBundleLocalGenerationWriterEvent(
        BundleLocalGenerationSlot(1, '1111'), _BundleExecutionDomainToken,
        0, BundleLocalGenerationCellCount(1));
    assert completed_writer_1;
    assert _LocalGenerations[[BundleLocalGenerationSlot(1, '1111')]].published;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0192 TMRGSORT source0 -> case_0192_normal_commit
func RunCase_0192() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 1 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                2, TileDataType_FP16, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                16, 4, 1,
                4, TileDataType_FP16,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0x26d19181, 32);
    assert started == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0818ce13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_0 = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert modifier_0_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(79);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(79);
    assert SelectedBundleClosedUnarySchemaLegal(79);
    assert SelectedBundleClosedTFMASchemaLegal(79);
    assert SelectedBundleClosedQuantizationSchemaLegal(79);
    assert SelectedBundleClosedGenerationSchemaLegal(79);
    assert SelectedBundleClosedHistogramSchemaLegal(79);
    assert SelectedBundleClosedReductionSchemaLegal(79);
    assert SelectedBundleClosedSortingSchemaLegal(79);
    assert SelectedBundleClosedPartialSchemaLegal(79);
    assert SelectedBundleClosedExpansionSchemaLegal(79);
    assert SelectedBundleClosedTCVTSchemaLegal(79);
    assert SelectedBundleClosedTCMPSchemaLegal(79);
    assert SelectedBundleClosedTSELSchemaLegal(79);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(79);
    assert SelectedBundleClosedTCMPSSchemaLegal(79);
    assert SelectedBundleClosedTSELSSchemaLegal(79);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(79);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    return TRUE;
end;

func main() => integer
begin
    let result_0189 = RunCase_0189();
    assert result_0189;
    let result_0190 = RunCase_0190();
    assert result_0190;
    let result_0191 = RunCase_0191();
    assert result_0191;
    let result_0192 = RunCase_0192();
    assert result_0192;
    return 0;
end;

// PTO-TEST: {"id":"PTO-AVS-BLOCK-RANGE-OPERATION-MATRIX-055","source":"asl/block/model/operands/subview-descriptor.asl","requirements":["PTO-B-SUBVIEW-DESCRIPTOR-001","PTO-BLOCK-TILE-OPERATION-APPLICABILITY-001","PTO-B-ASSEMBLE-PRODUCER-EFFECT-ELIGIBILITY-001"],"kind":"execution","summary":"Generated decoded operation-role cases exercise every required source and destination role through normal dispatch.","pass_condition":"Every case binds its authoritative fixture recipe and catalog-required operands/attributes, reaches normal modifier and applicability preflight, then asserts either its exact normal observable or its exact pre-effect fault.","related_sources":["asl/block/model/operands/subview-descriptor.asl","asl/block/model/operands/range-modifiers.asl","asl/block/model/operands/portable-carriers.asl"]}
// Generated by scripts/generate-bundle-operation-matrix.py; cases: RANGE-MATRIX-CASE-0217, RANGE-MATRIX-CASE-0218, RANGE-MATRIX-CASE-0219, RANGE-MATRIX-CASE-0220
// RANGE-MATRIX-CASE-0217 TMOV source0 -> case_0217_normal_commit
func RunCase_0217() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN});
    WriteGPR(3, Zeros{PTO_XLEN} + 8);
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    ConfigureTile(0, 128, 1, 4, 1, 4, TileDataType_FP16, TileLayout_RowMajor, TileLocation_Any);
    WriteTileElement(0, 0, 0, Zeros{PTO_XLEN} + 0x2a);
    InstallSharedTile((Zeros{6} + 16) as SharedTileID, _Tiles[[0]], '1111');
    let started = ExecuteCommandInstruction(Zeros{64} + 0x20c11181, 32);
    assert started == CommandExecution_Executed;
    let shared = ExecuteCommandInstruction(Zeros{64} + 0x01001e13, 32);
    assert shared == CommandExecution_Executed;
    let subview = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert subview == CommandExecution_Executed;
    let local = ExecuteCommandInstruction(Zeros{64} + 0x0008ee13, 32);
    assert local == CommandExecution_Executed;
    let completed = ExecuteBundleTileOperation();
    assert completed && _LastFault == Fault_None;
    let destination = _BundleTileBindings[[0]].destination;
    assert _Tiles[[destination]].contents_defined;
    assert ReadTileElement(destination, 0, 0) == Zeros{PTO_XLEN} + 0x2a;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0218 TMOV destination0 -> case_0218_normal_commit
func RunCase_0218() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN});
    WriteGPR(3, Zeros{PTO_XLEN} + 8);
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    ConfigureTile(1, 128, 1, 4, 1, 4, TileDataType_FP16, TileLayout_RowMajor, TileLocation_Any);
    WriteTileElement(1, 0, 0, Zeros{PTO_XLEN} + 0x2a);
    MarkTileValidRegionDefined(1);
    let started = ExecuteCommandInstruction(Zeros{64} + 0x20911181, 32);
    assert started == CommandExecution_Executed;
    let shared = ExecuteCommandInstruction(Zeros{64} + 0x01009e13, 32);
    assert shared == CommandExecution_Executed;
    let assemble = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble == CommandExecution_Executed;
    let local = ExecuteCommandInstruction(Zeros{64} + 0x00185e13, 32);
    assert local == CommandExecution_Executed;
    let completed = ExecuteBundleTileOperation();
    assert completed && _LastFault == Fault_None;
    assert SharedTileFullyInitialized((Zeros{6} + 16) as SharedTileID);
    assert ReadSharedTileWord((Zeros{6} + 16) as SharedTileID, 0) == Zeros{PTO_XLEN} + 0x2a;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0219 TPREFETCH operation -> case_0219_exact_fault
func RunCase_0219() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    let started = ExecuteCommandInstruction(Zeros{64} + 0x20311181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00310013, 32);
    assert ior == CommandExecution_Executed;
    let completed = ExecuteBundleTileOperation();
    assert !completed;
    assert _LastFault == Fault_DataAlignment;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0220 MGATHER source0 -> case_0220_exact_fault
func RunCase_0220() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    let started = ExecuteCommandInstruction(Zeros{64} + 0x20411181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x01001e13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_0 = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert modifier_0_0 == CommandExecution_Executed;
    let completed = ExecuteBundleTileOperation();
    assert !completed;
    assert _LastFault == Fault_TileLegality;
    return TRUE;
end;

func main() => integer
begin
    let result_0217 = RunCase_0217();
    assert result_0217;
    let result_0218 = RunCase_0218();
    assert result_0218;
    let result_0219 = RunCase_0219();
    assert result_0219;
    let result_0220 = RunCase_0220();
    assert result_0220;
    return 0;
end;

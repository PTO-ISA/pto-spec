// PTO-TEST: {"id":"PTO-AVS-BLOCK-RANGE-OPERATION-MATRIX-020","source":"asl/block/model/operands/subview-descriptor.asl","requirements":["PTO-B-SUBVIEW-DESCRIPTOR-001","PTO-BLOCK-TILE-OPERATION-APPLICABILITY-001","PTO-B-ASSEMBLE-PRODUCER-EFFECT-ELIGIBILITY-001"],"kind":"execution","summary":"Generated decoded operation-role cases exercise every required source and destination role through normal dispatch.","pass_condition":"Every case binds its authoritative fixture recipe and catalog-required operands/attributes, reaches normal modifier and applicability preflight, then asserts either its exact normal observable or its exact pre-effect fault.","related_sources":["asl/block/model/operands/subview-descriptor.asl","asl/block/model/operands/range-modifiers.asl","asl/block/model/operands/portable-carriers.asl"]}
// Generated by scripts/generate-bundle-operation-matrix.py; cases: RANGE-MATRIX-CASE-0077, RANGE-MATRIX-CASE-0078, RANGE-MATRIX-CASE-0079, RANGE-MATRIX-CASE-0080
// RANGE-MATRIX-CASE-0077 TREMS destination0 -> case_0077_normal_commit
func RunCase_0077() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 0 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_FP16, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                16, 4, 1,
                4, TileDataType_FP16,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0x22419181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0018de13, 32);
    assert bind_0 == CommandExecution_Executed;
    let assemble_0 = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(29);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(29);
    assert SelectedBundleClosedUnarySchemaLegal(29);
    assert SelectedBundleClosedTFMASchemaLegal(29);
    assert SelectedBundleClosedQuantizationSchemaLegal(29);
    assert SelectedBundleClosedGenerationSchemaLegal(29);
    assert SelectedBundleClosedHistogramSchemaLegal(29);
    assert SelectedBundleClosedReductionSchemaLegal(29);
    assert SelectedBundleClosedSortingSchemaLegal(29);
    assert SelectedBundleClosedPartialSchemaLegal(29);
    assert SelectedBundleClosedExpansionSchemaLegal(29);
    assert SelectedBundleClosedTCVTSchemaLegal(29);
    assert SelectedBundleClosedTCMPSchemaLegal(29);
    assert SelectedBundleClosedTSELSchemaLegal(29);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(29);
    assert SelectedBundleClosedTCMPSSchemaLegal(29);
    assert SelectedBundleClosedTSELSSchemaLegal(29);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(29);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    let completed_writer_0 = CompleteBundleLocalGenerationWriterEvent(
        BundleLocalGenerationSlot(0, '1111'), _BundleExecutionDomainToken,
        0, BundleLocalGenerationCellCount(1));
    assert completed_writer_0;
    assert _LocalGenerations[[BundleLocalGenerationSlot(0, '1111')]].published;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0078 TANDS source0 -> case_0078_normal_commit
func RunCase_0078() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 1 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xda619181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0018de13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_0 = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert modifier_0_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(30);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(30);
    assert SelectedBundleClosedUnarySchemaLegal(30);
    assert SelectedBundleClosedTFMASchemaLegal(30);
    assert SelectedBundleClosedQuantizationSchemaLegal(30);
    assert SelectedBundleClosedGenerationSchemaLegal(30);
    assert SelectedBundleClosedHistogramSchemaLegal(30);
    assert SelectedBundleClosedReductionSchemaLegal(30);
    assert SelectedBundleClosedSortingSchemaLegal(30);
    assert SelectedBundleClosedPartialSchemaLegal(30);
    assert SelectedBundleClosedExpansionSchemaLegal(30);
    assert SelectedBundleClosedTCVTSchemaLegal(30);
    assert SelectedBundleClosedTCMPSchemaLegal(30);
    assert SelectedBundleClosedTSELSchemaLegal(30);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(30);
    assert SelectedBundleClosedTCMPSSchemaLegal(30);
    assert SelectedBundleClosedTSELSSchemaLegal(30);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(30);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0079 TANDS destination0 -> case_0079_normal_commit
func RunCase_0079() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 0 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xda619181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0018de13, 32);
    assert bind_0 == CommandExecution_Executed;
    let assemble_0 = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(30);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(30);
    assert SelectedBundleClosedUnarySchemaLegal(30);
    assert SelectedBundleClosedTFMASchemaLegal(30);
    assert SelectedBundleClosedQuantizationSchemaLegal(30);
    assert SelectedBundleClosedGenerationSchemaLegal(30);
    assert SelectedBundleClosedHistogramSchemaLegal(30);
    assert SelectedBundleClosedReductionSchemaLegal(30);
    assert SelectedBundleClosedSortingSchemaLegal(30);
    assert SelectedBundleClosedPartialSchemaLegal(30);
    assert SelectedBundleClosedExpansionSchemaLegal(30);
    assert SelectedBundleClosedTCVTSchemaLegal(30);
    assert SelectedBundleClosedTCMPSchemaLegal(30);
    assert SelectedBundleClosedTSELSchemaLegal(30);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(30);
    assert SelectedBundleClosedTCMPSSchemaLegal(30);
    assert SelectedBundleClosedTSELSSchemaLegal(30);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(30);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    let completed_writer_0 = CompleteBundleLocalGenerationWriterEvent(
        BundleLocalGenerationSlot(0, '1111'), _BundleExecutionDomainToken,
        0, BundleLocalGenerationCellCount(1));
    assert completed_writer_0;
    assert _LocalGenerations[[BundleLocalGenerationSlot(0, '1111')]].published;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0080 TORS source0 -> case_0080_normal_commit
func RunCase_0080() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 1 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xda719181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let ior = ExecuteCommandInstruction(Zeros{64} + 0x00010013, 32);
    assert ior == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0018de13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_0 = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert modifier_0_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(31);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(31);
    assert SelectedBundleClosedUnarySchemaLegal(31);
    assert SelectedBundleClosedTFMASchemaLegal(31);
    assert SelectedBundleClosedQuantizationSchemaLegal(31);
    assert SelectedBundleClosedGenerationSchemaLegal(31);
    assert SelectedBundleClosedHistogramSchemaLegal(31);
    assert SelectedBundleClosedReductionSchemaLegal(31);
    assert SelectedBundleClosedSortingSchemaLegal(31);
    assert SelectedBundleClosedPartialSchemaLegal(31);
    assert SelectedBundleClosedExpansionSchemaLegal(31);
    assert SelectedBundleClosedTCVTSchemaLegal(31);
    assert SelectedBundleClosedTCMPSchemaLegal(31);
    assert SelectedBundleClosedTSELSchemaLegal(31);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(31);
    assert SelectedBundleClosedTCMPSSchemaLegal(31);
    assert SelectedBundleClosedTSELSSchemaLegal(31);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(31);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    return TRUE;
end;

func main() => integer
begin
    let result_0077 = RunCase_0077();
    assert result_0077;
    let result_0078 = RunCase_0078();
    assert result_0078;
    let result_0079 = RunCase_0079();
    assert result_0079;
    let result_0080 = RunCase_0080();
    assert result_0080;
    return 0;
end;

// PTO-TEST: {"id":"PTO-AVS-BLOCK-RANGE-OPERATION-MATRIX-006","source":"asl/block/model/operands/subview-descriptor.asl","requirements":["PTO-B-SUBVIEW-DESCRIPTOR-001","PTO-BLOCK-TILE-OPERATION-APPLICABILITY-001","PTO-B-ASSEMBLE-PRODUCER-EFFECT-ELIGIBILITY-001"],"kind":"execution","summary":"Generated decoded operation-role cases exercise every required source and destination role through normal dispatch.","pass_condition":"Every case binds its authoritative fixture recipe and catalog-required operands/attributes, reaches normal modifier and applicability preflight, then asserts either its exact normal observable or its exact pre-effect fault.","related_sources":["asl/block/model/operands/subview-descriptor.asl","asl/block/model/operands/range-modifiers.asl","asl/block/model/operands/portable-carriers.asl"]}
// Generated by scripts/generate-bundle-operation-matrix.py; cases: RANGE-MATRIX-CASE-0021, RANGE-MATRIX-CASE-0022, RANGE-MATRIX-CASE-0023, RANGE-MATRIX-CASE-0024
// RANGE-MATRIX-CASE-0021 TOR destination0 -> case_0021_normal_commit
func RunCase_0021() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 0 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xd8719181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0818ce13, 32);
    assert bind_0 == CommandExecution_Executed;
    let assemble_0 = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(6);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(6);
    assert SelectedBundleClosedUnarySchemaLegal(6);
    assert SelectedBundleClosedTFMASchemaLegal(6);
    assert SelectedBundleClosedQuantizationSchemaLegal(6);
    assert SelectedBundleClosedGenerationSchemaLegal(6);
    assert SelectedBundleClosedHistogramSchemaLegal(6);
    assert SelectedBundleClosedReductionSchemaLegal(6);
    assert SelectedBundleClosedSortingSchemaLegal(6);
    assert SelectedBundleClosedPartialSchemaLegal(6);
    assert SelectedBundleClosedExpansionSchemaLegal(6);
    assert SelectedBundleClosedTCVTSchemaLegal(6);
    assert SelectedBundleClosedTCMPSchemaLegal(6);
    assert SelectedBundleClosedTSELSchemaLegal(6);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(6);
    assert SelectedBundleClosedTCMPSSchemaLegal(6);
    assert SelectedBundleClosedTSELSSchemaLegal(6);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(6);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    let completed_writer_0 = CompleteBundleLocalGenerationWriterEvent(
        BundleLocalGenerationSlot(0, '1111'), _BundleExecutionDomainToken,
        0, BundleLocalGenerationCellCount(1));
    assert completed_writer_0;
    assert _LocalGenerations[[BundleLocalGenerationSlot(0, '1111')]].published;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0022 TXOR source0 -> case_0022_normal_commit
func RunCase_0022() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 1 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xd8819181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0818ce13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_0 = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert modifier_0_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(7);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(7);
    assert SelectedBundleClosedUnarySchemaLegal(7);
    assert SelectedBundleClosedTFMASchemaLegal(7);
    assert SelectedBundleClosedQuantizationSchemaLegal(7);
    assert SelectedBundleClosedGenerationSchemaLegal(7);
    assert SelectedBundleClosedHistogramSchemaLegal(7);
    assert SelectedBundleClosedReductionSchemaLegal(7);
    assert SelectedBundleClosedSortingSchemaLegal(7);
    assert SelectedBundleClosedPartialSchemaLegal(7);
    assert SelectedBundleClosedExpansionSchemaLegal(7);
    assert SelectedBundleClosedTCVTSchemaLegal(7);
    assert SelectedBundleClosedTCMPSchemaLegal(7);
    assert SelectedBundleClosedTSELSchemaLegal(7);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(7);
    assert SelectedBundleClosedTCMPSSchemaLegal(7);
    assert SelectedBundleClosedTSELSSchemaLegal(7);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(7);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0023 TXOR source1 -> case_0023_normal_commit
func RunCase_0023() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 2 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xd8819181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0818ce13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_1 = ExecuteCommandInstruction(Zeros{64} + 0x800000d3, 32);
    assert modifier_0_1 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(7);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(7);
    assert SelectedBundleClosedUnarySchemaLegal(7);
    assert SelectedBundleClosedTFMASchemaLegal(7);
    assert SelectedBundleClosedQuantizationSchemaLegal(7);
    assert SelectedBundleClosedGenerationSchemaLegal(7);
    assert SelectedBundleClosedHistogramSchemaLegal(7);
    assert SelectedBundleClosedReductionSchemaLegal(7);
    assert SelectedBundleClosedSortingSchemaLegal(7);
    assert SelectedBundleClosedPartialSchemaLegal(7);
    assert SelectedBundleClosedExpansionSchemaLegal(7);
    assert SelectedBundleClosedTCVTSchemaLegal(7);
    assert SelectedBundleClosedTCMPSchemaLegal(7);
    assert SelectedBundleClosedTSELSchemaLegal(7);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(7);
    assert SelectedBundleClosedTCMPSSchemaLegal(7);
    assert SelectedBundleClosedTSELSSchemaLegal(7);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(7);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0024 TXOR destination0 -> case_0024_normal_commit
func RunCase_0024() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 0 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xd8819181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0818ce13, 32);
    assert bind_0 == CommandExecution_Executed;
    let assemble_0 = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(7);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(7);
    assert SelectedBundleClosedUnarySchemaLegal(7);
    assert SelectedBundleClosedTFMASchemaLegal(7);
    assert SelectedBundleClosedQuantizationSchemaLegal(7);
    assert SelectedBundleClosedGenerationSchemaLegal(7);
    assert SelectedBundleClosedHistogramSchemaLegal(7);
    assert SelectedBundleClosedReductionSchemaLegal(7);
    assert SelectedBundleClosedSortingSchemaLegal(7);
    assert SelectedBundleClosedPartialSchemaLegal(7);
    assert SelectedBundleClosedExpansionSchemaLegal(7);
    assert SelectedBundleClosedTCVTSchemaLegal(7);
    assert SelectedBundleClosedTCMPSchemaLegal(7);
    assert SelectedBundleClosedTSELSchemaLegal(7);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(7);
    assert SelectedBundleClosedTCMPSSchemaLegal(7);
    assert SelectedBundleClosedTSELSSchemaLegal(7);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(7);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    let completed_writer_0 = CompleteBundleLocalGenerationWriterEvent(
        BundleLocalGenerationSlot(0, '1111'), _BundleExecutionDomainToken,
        0, BundleLocalGenerationCellCount(1));
    assert completed_writer_0;
    assert _LocalGenerations[[BundleLocalGenerationSlot(0, '1111')]].published;
    return TRUE;
end;

func main() => integer
begin
    let result_0021 = RunCase_0021();
    assert result_0021;
    let result_0022 = RunCase_0022();
    assert result_0022;
    let result_0023 = RunCase_0023();
    assert result_0023;
    let result_0024 = RunCase_0024();
    assert result_0024;
    return 0;
end;

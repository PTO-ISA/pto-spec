// PTO-TEST: {"id":"PTO-AVS-BLOCK-RANGE-OPERATION-MATRIX-011","source":"asl/block/model/operands/subview-descriptor.asl","requirements":["PTO-B-SUBVIEW-DESCRIPTOR-001","PTO-BLOCK-TILE-OPERATION-APPLICABILITY-001","PTO-B-ASSEMBLE-PRODUCER-EFFECT-ELIGIBILITY-001"],"kind":"execution","summary":"Generated decoded operation-role cases exercise every required source and destination role through normal dispatch.","pass_condition":"Every case binds its authoritative fixture recipe and catalog-required operands/attributes, reaches normal modifier and applicability preflight, then asserts either its exact normal observable or its exact pre-effect fault.","related_sources":["asl/block/model/operands/subview-descriptor.asl","asl/block/model/operands/range-modifiers.asl","asl/block/model/operands/portable-carriers.asl"]}
// Generated by scripts/generate-bundle-operation-matrix.py; cases: RANGE-MATRIX-CASE-0041, RANGE-MATRIX-CASE-0042, RANGE-MATRIX-CASE-0043, RANGE-MATRIX-CASE-0044
// RANGE-MATRIX-CASE-0041 TABS destination0 -> case_0041_normal_commit
func RunCase_0041() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 0 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_FP16, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                16, 4, 1,
                4, TileDataType_FP16,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0x20f19181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0018de13, 32);
    assert bind_0 == CommandExecution_Executed;
    let assemble_0 = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(13);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(13);
    assert SelectedBundleClosedUnarySchemaLegal(13);
    assert SelectedBundleClosedTFMASchemaLegal(13);
    assert SelectedBundleClosedQuantizationSchemaLegal(13);
    assert SelectedBundleClosedGenerationSchemaLegal(13);
    assert SelectedBundleClosedHistogramSchemaLegal(13);
    assert SelectedBundleClosedReductionSchemaLegal(13);
    assert SelectedBundleClosedSortingSchemaLegal(13);
    assert SelectedBundleClosedPartialSchemaLegal(13);
    assert SelectedBundleClosedExpansionSchemaLegal(13);
    assert SelectedBundleClosedTCVTSchemaLegal(13);
    assert SelectedBundleClosedTCMPSchemaLegal(13);
    assert SelectedBundleClosedTSELSchemaLegal(13);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(13);
    assert SelectedBundleClosedTCMPSSchemaLegal(13);
    assert SelectedBundleClosedTSELSSchemaLegal(13);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(13);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    let completed_writer_0 = CompleteBundleLocalGenerationWriterEvent(
        BundleLocalGenerationSlot(0, '1111'), _BundleExecutionDomainToken,
        0, BundleLocalGenerationCellCount(1));
    assert completed_writer_0;
    assert _LocalGenerations[[BundleLocalGenerationSlot(0, '1111')]].published;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0042 TNOT source0 -> case_0042_normal_commit
func RunCase_0042() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 1 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xd9019181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0018de13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_0 = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert modifier_0_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(14);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(14);
    assert SelectedBundleClosedUnarySchemaLegal(14);
    assert SelectedBundleClosedTFMASchemaLegal(14);
    assert SelectedBundleClosedQuantizationSchemaLegal(14);
    assert SelectedBundleClosedGenerationSchemaLegal(14);
    assert SelectedBundleClosedHistogramSchemaLegal(14);
    assert SelectedBundleClosedReductionSchemaLegal(14);
    assert SelectedBundleClosedSortingSchemaLegal(14);
    assert SelectedBundleClosedPartialSchemaLegal(14);
    assert SelectedBundleClosedExpansionSchemaLegal(14);
    assert SelectedBundleClosedTCVTSchemaLegal(14);
    assert SelectedBundleClosedTCMPSchemaLegal(14);
    assert SelectedBundleClosedTSELSchemaLegal(14);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(14);
    assert SelectedBundleClosedTCMPSSchemaLegal(14);
    assert SelectedBundleClosedTSELSSchemaLegal(14);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(14);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0043 TNOT destination0 -> case_0043_normal_commit
func RunCase_0043() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 0 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_U8, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                32, 4, 1,
                4, TileDataType_U8,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0xd9019181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0018de13, 32);
    assert bind_0 == CommandExecution_Executed;
    let assemble_0 = ExecuteCommandInstruction(Zeros{64} + 0x800018d3, 32);
    assert assemble_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(14);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(14);
    assert SelectedBundleClosedUnarySchemaLegal(14);
    assert SelectedBundleClosedTFMASchemaLegal(14);
    assert SelectedBundleClosedQuantizationSchemaLegal(14);
    assert SelectedBundleClosedGenerationSchemaLegal(14);
    assert SelectedBundleClosedHistogramSchemaLegal(14);
    assert SelectedBundleClosedReductionSchemaLegal(14);
    assert SelectedBundleClosedSortingSchemaLegal(14);
    assert SelectedBundleClosedPartialSchemaLegal(14);
    assert SelectedBundleClosedExpansionSchemaLegal(14);
    assert SelectedBundleClosedTCVTSchemaLegal(14);
    assert SelectedBundleClosedTCMPSchemaLegal(14);
    assert SelectedBundleClosedTSELSchemaLegal(14);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(14);
    assert SelectedBundleClosedTCMPSSchemaLegal(14);
    assert SelectedBundleClosedTSELSSchemaLegal(14);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(14);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    let completed_writer_0 = CompleteBundleLocalGenerationWriterEvent(
        BundleLocalGenerationSlot(0, '1111'), _BundleExecutionDomainToken,
        0, BundleLocalGenerationCellCount(1));
    assert completed_writer_0;
    assert _LocalGenerations[[BundleLocalGenerationSlot(0, '1111')]].published;
    return TRUE;
end;

// RANGE-MATRIX-CASE-0044 TNEG source0 -> case_0044_normal_commit
func RunCase_0044() => boolean
begin
    ResetProfileState();
    WriteGPR(0, Zeros{PTO_XLEN});
    WriteGPR(2, Zeros{PTO_XLEN} + 1);
    WriteGPR(3, Zeros{PTO_XLEN});
    WriteGPR(4, Zeros{PTO_XLEN} + 1);
    for tile = 1 to 8 looplimit 8 do
        if tile == 1 then
            let configured =
            ConfigureCubeTileForMask(tile, 128, 1,
                4, TileDataType_FP16, TileLayout_CUBE_M16,
                TileLocation_Matrix, '1111');
            assert configured;
        else
            ConfigureTileForMask(tile, 128,
                16, 4, 1,
                4, TileDataType_FP16,
                TileLayout_RowMajor, TileLocation_Any, '1111');
        end;
        MarkTileValidRegionDefined(tile);
    end;
    let started = ExecuteCommandInstruction(Zeros{64} + 0x21119181, 32);
    assert started == CommandExecution_Executed;
    let dim_400043 = ExecuteCommandInstruction(Zeros{64} + 0x00400043, 32);
    assert dim_400043 == CommandExecution_Executed;
    let dim_101043 = ExecuteCommandInstruction(Zeros{64} + 0x00101043, 32);
    assert dim_101043 == CommandExecution_Executed;
    let dim_402043 = ExecuteCommandInstruction(Zeros{64} + 0x00402043, 32);
    assert dim_402043 == CommandExecution_Executed;
    let bind_0 = ExecuteCommandInstruction(Zeros{64} + 0x0018de13, 32);
    assert bind_0 == CommandExecution_Executed;
    let modifier_0_0 = ExecuteCommandInstruction(Zeros{64} + 0x000000d3, 32);
    assert modifier_0_0 == CommandExecution_Executed;
    assert BundleOperationBindingsComplete(15);
    let prepared_stage2 = PrepareSelectedBundleStage2();
    assert prepared_stage2;
    assert SelectedBundleClosedBinarySchemaLegal(15);
    assert SelectedBundleClosedUnarySchemaLegal(15);
    assert SelectedBundleClosedTFMASchemaLegal(15);
    assert SelectedBundleClosedQuantizationSchemaLegal(15);
    assert SelectedBundleClosedGenerationSchemaLegal(15);
    assert SelectedBundleClosedHistogramSchemaLegal(15);
    assert SelectedBundleClosedReductionSchemaLegal(15);
    assert SelectedBundleClosedSortingSchemaLegal(15);
    assert SelectedBundleClosedPartialSchemaLegal(15);
    assert SelectedBundleClosedExpansionSchemaLegal(15);
    assert SelectedBundleClosedTCVTSchemaLegal(15);
    assert SelectedBundleClosedTCMPSchemaLegal(15);
    assert SelectedBundleClosedTSELSchemaLegal(15);
    assert SelectedBundleClosedTileScalarBinarySchemaLegal(15);
    assert SelectedBundleClosedTCMPSSchemaLegal(15);
    assert SelectedBundleClosedTSELSSchemaLegal(15);
    assert SelectedBundleClosedTEXPANDSSchemaLegal(15);
    assert SelectedBundleTileMasksLegal();
    let completed = ExecuteBundleTileOperation();
    assert completed;
    if !completed then assert _LastFault == Fault_TileLegality; end;
    assert _LastFault == Fault_None;
    var observed_destination: TileIndex = 0;
    for binding = 0 to PTO_BUNDLE_TILE_BINDING_COUNT - 1 do
        if _BundleTileBindings[[binding]].valid &&
           _BundleTileBindings[[binding]].destination_valid then
            observed_destination = _BundleTileBindings[[binding]].destination;
        end;
    end;
    assert _Tiles[[observed_destination]].allocated &&
           _Tiles[[observed_destination]].contents_defined;
    return TRUE;
end;

func main() => integer
begin
    let result_0041 = RunCase_0041();
    assert result_0041;
    let result_0042 = RunCase_0042();
    assert result_0042;
    let result_0043 = RunCase_0043();
    assert result_0043;
    let result_0044 = RunCase_0044();
    assert result_0044;
    return 0;
end;

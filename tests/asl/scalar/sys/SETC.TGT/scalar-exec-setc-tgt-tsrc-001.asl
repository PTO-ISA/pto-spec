// Generated by scripts/generate-mnemonic-avs.py. Do not edit.
// PTO-TEST: {"id":"PTO-AVS-SYS-SETC-TGT-TSRC-001","source":"asl/scalar/sys/SETC.TGT.asl","requirements":["PTO-INST-SCALAR-SETC-TGT"],"kind":"execution","summary":"SETC.TGT proves its tsrc SYS contract","pass_condition":"the mnemonic-owned decoded assertions hold","related_sources":[]}
func main() => integer
begin
    ResetProfileState();
    SetCurrentACR(0);
    WriteTPC(Zeros{PTO_XLEN} + 0x100);
    BeginBundle(BundleKind_Standard, BundleTransfer_Fallthrough,
        Zeros{PTO_XLEN}, Zeros{PTO_XLEN} + 0x180,
        Zeros{PTO_XLEN}, FALSE);
    EnterBundleBody();
    _TQueue[[0]] = Zeros{PTO_XLEN} + 0x1000;
    _TQueueValid[[0]] = TRUE;
    let status = ExecuteScalarInstruction('000000000000000000000000000011000100000000111011', 32);
    assert status == ScalarExecution_Executed;
    assert _LastFault == Fault_None;
    assert ReadBPC() == Zeros{PTO_XLEN} + 0x100;
    assert _BARG.block_type == BundleKind_Standard;
    assert _BARG.transfer_type == BundleTransfer_Fallthrough;
    assert !_BARG.taken;
    assert _BARG.bpcn == Zeros{PTO_XLEN} + 0x1000;
    assert ReadTemporaryQueue(TRUE, 0) == Zeros{PTO_XLEN} + 0x1000;
    assert ReadTPC() == Zeros{PTO_XLEN} + 0x184;
    assert InstructionContractOperation_SETC_TGT() == ScalarOperation_SETC_TGT;
    return 0;
end;

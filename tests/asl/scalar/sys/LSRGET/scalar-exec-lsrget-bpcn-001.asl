// Generated by scripts/generate-mnemonic-avs.py. Do not edit.
// PTO-TEST: {"id":"PTO-AVS-SYS-LSRGET-BPCN-001","source":"asl/scalar/sys/LSRGET.asl","requirements":["PTO-INST-SCALAR-LSRGET"],"kind":"execution","summary":"LSRGET proves its bpcn SYS contract","pass_condition":"the mnemonic-owned decoded assertions hold","related_sources":[]}
func main() => integer
begin
    ResetProfileState();
    SetCurrentACR(0);
    WriteTPC(Zeros{PTO_XLEN} + 0x100);
    BeginBundle(BundleKind_Standard, BundleTransfer_Direct,
        Zeros{PTO_XLEN} + 0x1000, Zeros{PTO_XLEN} + 0x180,
        Zeros{PTO_XLEN}, FALSE);
    EnterBundleBody();
    WriteGPR(3, Zeros{PTO_XLEN} + 0x333);
    let status = ExecuteScalarInstruction('000000000000000000000000000100000011000110111011', 32);
    assert status == ScalarExecution_Executed;
    assert _LastFault == Fault_None;
    assert ReadGPR(3) == Zeros{PTO_XLEN} + 0x1000;
    assert ReadBPC() == Zeros{PTO_XLEN} + 0x100;
    assert _BARG.block_type == BundleKind_Standard;
    assert _BARG.transfer_type == BundleTransfer_Direct;
    assert _BARG.taken == FALSE;
    assert _BARG.bpcn == Zeros{PTO_XLEN} + 0x1000;
    assert ReadTPC() == Zeros{PTO_XLEN} + 0x184;
    return 0;
end;

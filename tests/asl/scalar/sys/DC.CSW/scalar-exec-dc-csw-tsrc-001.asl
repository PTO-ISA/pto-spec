// Generated by scripts/generate-mnemonic-avs.py. Do not edit.
// PTO-TEST: {"id":"PTO-AVS-SYS-DC-CSW-TSRC-001","source":"asl/scalar/sys/DC.CSW.asl","requirements":["PTO-INST-SCALAR-DC-CSW"],"kind":"execution","summary":"DC.CSW proves its tsrc SYS contract","pass_condition":"the mnemonic-owned decoded assertions hold","related_sources":[]}
func main() => integer
begin
    ResetProfileState();
    SetCurrentACR(0);
    WriteTPC(Zeros{PTO_XLEN} + 0x100);
    BeginBundle(BundleKind_System, BundleTransfer_Fallthrough,
        Zeros{PTO_XLEN}, Zeros{PTO_XLEN} + 0x180,
        Zeros{PTO_XLEN}, FALSE);
    EnterBundleBody();
    _TQueue[[0]] = Zeros{PTO_XLEN} + 0x1000;
    _TQueueValid[[0]] = TRUE;
    let status = ExecuteScalarInstruction('000000000000000000000000010111000110000000101011', 32);
    assert status == ScalarExecution_Executed;
    assert _LastFault == Fault_None;
    assert _DataCacheEpoch == 1;
    assert ReadTemporaryQueue(TRUE, 0) == Zeros{PTO_XLEN} + 0x1000;
    assert _LastMaintenanceOperation == Maintenance_DC_CSW;
    assert _LastMaintenanceOperand == Zeros{PTO_XLEN} + 0x1000;
    assert ReadTPC() == Zeros{PTO_XLEN} + 0x184;
    assert InstructionContractOperation_DC_CSW() == ScalarOperation_DC_CSW;
    return 0;
end;
